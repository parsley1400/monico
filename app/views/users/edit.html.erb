<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="assets/stylesheets/user.scss">
</head>
<body id="edit_body">
<div class="wrapper">
<a href="/users/<%= @user.id %>"><i class="fas fa-chevron-left arrow"></i></a>

  <%= form_for(@user) do |f| %>
  <div class="form-group new_form">
    <%= f.label :image, '画像：' %>
    <%= f.file_field :image %>
  </div>

  <div class="form-group new_form">
    <%= f.label :name, 'ユーザーネーム：' %>
    <%= f.text_field :name, class: 'new_text' %>
  </div>

  <div class="form-group new_form">
    <%= f.label :call, '電話番号：' %>
    <%= f.text_field :call, :placeholder => "ex)000-0000-0000", class: 'new_text' %>
  </div>

  <%= f.submit '更新', class: 'submit' %>
<% end %>

<a href="delete" class="delete_link">アカウント削除</a>
</div>
</body>
</html>